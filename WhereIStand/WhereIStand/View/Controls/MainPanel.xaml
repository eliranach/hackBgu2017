<UserControl x:Class="WhereIStand.View.Controls.MainPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhereIStand.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="1" Orientation="Horizontal">
            <Button Name="connectButton" Content="Connect" Background="DarkCyan" Foreground="White" Style="{StaticResource BaseButtonStyle}" VerticalAlignment="Top"  Click="connectClick" Grid.Row="1" HorizontalAlignment="Center"/>

        </WrapPanel>

        <WebBrowser x:Name="wb" Grid.Row="2" ></WebBrowser>
    </Grid>
</UserControl>
